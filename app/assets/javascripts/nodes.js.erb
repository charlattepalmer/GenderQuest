$(function () {
  //console.log("DOCUMENT READY");
  //console.log("SCORE IS " + score );
  $(".restrict").each(function() {
    var min_score = $(this).data("min-score")
    //console.log("MIN SCORE IS " + min_score );
    if (score < min_score) {
      var $a = $(this).prev("p").children("a");
      $a.addClass("restricted");
    }
  });
});